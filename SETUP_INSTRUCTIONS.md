# Инструкция по настройке базы данных

## Шаг 1: Настройка переменных окружения

1. Откройте файл `.env`
2. Найдите `SUPABASE_ANON_KEY=YOUR_ANON_KEY_HERE`
3. Замените `YOUR_ANON_KEY_HERE` на ваш Supabase Anon Key

**Где найти Anon Key:**
- Откройте Supabase Dashboard: https://supabase.com/dashboard
- Выберите ваш проект (sxwikzepzdcjapbrgymw)
- Перейдите в Settings → API
- Скопируйте "anon public" ключ

## Шаг 2: Создание таблицы

1. Откройте Supabase Dashboard
2. Перейдите в SQL Editor
3. Скопируйте содержимое файла `database_setup.sql`
4. Вставьте в SQL Editor и нажмите "Run"

**ИЛИ** просто скопируйте этот SQL:

```sql
CREATE TABLE dictionary (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  ce TEXT NOT NULL,
  ru TEXT NOT NULL,
  type TEXT NOT NULL CHECK (type IN ('word', 'phrase')),
  category TEXT,
  is_verified BOOLEAN DEFAULT true,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  UNIQUE(ce)
);

CREATE INDEX idx_dictionary_type ON dictionary(type);
CREATE INDEX idx_dictionary_category ON dictionary(category);
CREATE INDEX idx_dictionary_ce ON dictionary(ce);
```

## Шаг 3: Загрузка данных

После создания таблицы запустите:

```bash
npm run setup
```

### Как работает синхронизация

Скрипт использует **UPSERT** - умное обновление:
- ✅ Обновляет существующие записи (по полю `ce`)
- ✅ Добавляет новые записи
- ✅ **НЕ удаляет** существующие данные

Это позволяет:
- Дополнять базу новыми словами/фразами
- Исправлять переводы существующих записей
- Безопасно запускать скрипт многократно

### Полная очистка (если нужно начать с нуля)

```bash
npm run setup:clean
```

⚠️ **Внимание:** Это удалит **ВСЕ** записи из базы!

### Что делает скрипт

- Проверяет подключение к Supabase
- (Опционально) Очищает таблицу при флаге `--clean`
- Загружает данные из `master_seed.json`:
  - Слова (26 записей)
  - Глаголы (6 записей)
  - Фразы (все фразы из JSON)

## Результат

После успешного выполнения вы увидите:

```
✅ Успешно синхронизировано X записей!
📈 Всего записей в базе: X
   - Слов и глаголов: X
   - Фраз: X
```

Каждый следующий запуск `npm run setup`:
- Обновит изменённые переводы
- Добавит новые слова/фразы из master_seed.json
- Сохранит все существующие записи

## Проверка

Откройте Supabase Dashboard → Table Editor → dictionary
Вы должны увидеть все загруженные записи.
