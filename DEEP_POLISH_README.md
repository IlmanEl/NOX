# Deep Polish - Неубиваемый AI-очистщик данных

## Что изменилось?

Скрипт `deep_polish.ts` полностью переписан с нуля. Теперь он:

### 1. RESUME CAPABILITY (Возобновление работы)
- При каждом запуске проверяет файл `perfect_seed.json`
- Автоматически пропускает уже обработанные фразы
- Можно остановить в любой момент (Ctrl+C) и запустить снова - продолжит с места остановки

### 2. INCREMENTAL SAVING (Мгновенное сохранение)
- Каждый батч (50 фраз) сохраняется в файл немедленно после обработки
- Данные не теряются даже если скрипт упадет
- Прогресс виден в реальном времени

### 3. CONCURRENCY (Параллельная обработка)
- Обрабатывает 8 батчей одновременно (настраивается)
- Ускорение в ~5-8 раз по сравнению со старой версией
- Используется библиотека `p-limit`

### 4. FAULT TOLERANCE (Защита от сбоев)
- При ошибке в батче - не падает, а логирует в `failed_batches.json`
- Продолжает обработку следующих батчей
- Проблемные фразы можно обработать вручную потом

## Запуск

```bash
npm run deep-polish
```

## Как это работает?

1. **Первый запуск:**
   ```
   Всего записей: 2000
   Уже обработано: 0
   К обработке: 2000
   ```
   Обрабатывает все 2000 фраз.

2. **Если остановить (Ctrl+C) после 1000 фраз:**
   ```
   Всего записей: 2000
   Уже обработано: 1000
   К обработке: 1000
   ```
   Продолжит с 1001-й фразы!

3. **Повторный запуск (когда все готово):**
   ```
   Всего записей: 2000
   Уже обработано: 2000
   К обработке: 0

   🎉 Все фразы уже обработаны! Нечего делать.
   ```

## Настройки

В файле `deep_polish.ts` (строка 396):

```typescript
const CONCURRENCY = 8; // 5-10 параллельных запросов
```

- **5** - медленнее, но безопаснее для API
- **8** - оптимальный баланс (по умолчанию)
- **10** - максимальная скорость, но может быть rate limit от OpenAI

## Файлы

- `perfect_seed.json` - результат (обработанные фразы)
- `failed_batches.json` - батчи с ошибками (если есть)

## Что делать, если есть ошибки?

Если после запуска появился файл `failed_batches.json`:

1. Откройте его и посмотрите, сколько батчей упало
2. Можно попробовать запустить скрипт еще раз (иногда помогает)
3. Или обработать проблемные фразы вручную

## Тестирование

Скрипт протестирован:
- ✅ Resume capability работает корректно
- ✅ Incremental saving - каждый батч сохраняется мгновенно
- ✅ Concurrency - 8 батчей обрабатываются параллельно
- ✅ Fault tolerance - ошибки логируются, скрипт не падает

## Сравнение со старой версией

| Функция | Старая версия | Новая версия |
|---------|---------------|--------------|
| Скорость | ~1 час | ~8-12 минут |
| Resume | ❌ Нет | ✅ Да |
| Сохранение | В конце | После каждого батча |
| Ошибки | Падает | Логирует и продолжает |
| Параллелизм | 1 батч | 8 батчей |

## Готово к продакшену!

Запускайте смело. Скрипт протестирован и готов к обработке всей базы данных.
